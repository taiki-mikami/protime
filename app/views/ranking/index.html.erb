<%= render 'layouts/header' %>

<div class="container">
    <div class="row">
        <div class="col-sm-6 offset-sm-3">
            <% @study_records.each do |studyrecord| %>
                    <ul>
                        <li>
                            <%= studyrecord.studytime %>
                        </li>
                </ul>
            <% end %>
        </div>
    </div>
</div>
